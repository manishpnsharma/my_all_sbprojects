server:
  port: 8889
spring:
  application:
    name: spring_cloude_config_server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/manishpnsharma/Education_backend.git
